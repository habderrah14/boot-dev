# Mini-project — 13-generics

_Companion chapter:_ [`13-generics.md`](../13-generics.md)

**Goal.** Build a typed `Cache<K, V>` class with `get`, `set`, `delete`, and `size`. Generic over both K (key) and V (value) with a default `K = string`.
