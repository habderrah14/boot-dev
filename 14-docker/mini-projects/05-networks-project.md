# Mini-project — 05-networks

_Companion chapter:_ [`05-networks.md`](../05-networks.md)

**Goal.** Compose file with `api`, `db`, `worker`, `redis`. Split into two networks: `frontend` (api) and `backend` (db, worker, redis). Verify the segregation with `exec` tests.
